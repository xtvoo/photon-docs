# type

Internal types used by the API.

## Types

- `vector2 (x, y)`
- `vector3 (x, y, z)`
- `color (r, g, b, a)`
- `instance (Roblox object pointer)`

## Vector2 Functions

- `add, subtract, multiply_scalar, length, distance, out_of_screen, lerp, unpack, type`
