# render

Functions to render objects on screen.

## Functions

- `render add_text(text, position, size, color, centered, outline)`
- `render add_line(point_a, point_b, color, thickness)`
- `render add_rect(position, size, color, thickness, rounding)`
- `render add_circle(position, radius, color, segments, thickness)`
- `render add_image(path, position, size, color)`
- `render create_font(path, size, ranges)`
