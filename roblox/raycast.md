# raycast

Functions to cast a ray for roblox objects. Max distance 6000 studs.

**Fields**: hit (bool), distance (float), origin (vector3), hit_part (instance), hit_point (vector3)

## Function

- `raycast send(point_a, point_b, ignore) -> result_table`
